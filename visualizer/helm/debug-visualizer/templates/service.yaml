apiVersion: v1
kind: Service
metadata:
  name: {{ include "debug-visualizer.fullname" . }}
  labels:
    {{- include "debug-visualizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "debug-visualizer.selectorLabels" . | nindent 4 }}

