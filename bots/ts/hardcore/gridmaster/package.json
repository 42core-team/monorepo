{
  "name": "hardcore-gridmaster",
  "version": "1.0.0",
  "description": "Hardcore Gridmaster TS bot",
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build index.ts --target=bun --outfile dist/index.js"
  },
  "dependencies": {
    "@core/client-lib": "file:/core/client_lib/"
  }
}
